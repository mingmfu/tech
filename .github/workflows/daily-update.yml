name: Daily Content Update (Production)

on:
  schedule:
    - cron: '0 2 * * *'  # 每天北京时间10:00 (UTC 2:00)
  workflow_dispatch:

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: pip install requests
      
      - name: Update all content
        run: |
          echo "🚀 开始统一更新所有栏目..."
          python3 update_all.py
      
      - name: Trigger Pages rebuild
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.repos.requestPagesBuild({
              owner: context.repo.owner,
              repo: context.repo.repo
            })
      
      - name: Notify success
        run: |
          echo "✅ 每日更新完成!"
          echo "📅 日期: $(date +'%Y-%m-%d')"
          echo "🌐 网站: https://mingmfu.github.io/tech/"
          echo "📱 API: https://mingmfu.github.io/tech/api/tech-news.json"
