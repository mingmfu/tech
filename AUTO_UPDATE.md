# TechInsight Hub - 自动更新系统

## 🔄 每日自动更新

网站已配置 GitHub Actions 自动更新，每天北京时间 **10:00** 自动获取最新内容。

### 更新内容

- **AI热点新闻** - 每日最新AI产业动态
- **学术前沿** - 最新论文和研究进展
- **日期更新** - 自动更新"最后更新"时间

### 手动触发更新

如果需要立即更新：

1. 进入 GitHub 仓库
2. 点击 **Actions** 标签
3. 选择 **Daily Content Update**
4. 点击 **Run workflow**

## 🛠️ 系统架构

```
GitHub Actions (每天 10:00 UTC+8)
    │
    ├── 获取最新AI新闻
    ├── 获取最新学术论文
    ├── 更新 index.html
    ├── 提交到 GitHub
    └── 触发 GitHub Pages 部署
```

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `.github/workflows/daily-update.yml` | GitHub Actions 工作流配置 |
| `scripts/update_daily.py` | 本地更新脚本（可选） |
| `index.html` | 主页面（自动更新） |

## 🔧 本地测试更新

```bash
cd ~/tech

# 安装依赖
pip install requests beautifulsoup4

# 运行更新脚本
python3 scripts/update_daily.py

# 查看更改
git diff

# 提交（如需）
git add index.html
git commit -m "Manual content update"
git push
```

## 📝 自定义内容源

编辑 `.github/workflows/daily-update.yml` 中的 `Fetch latest AI news` 步骤：

```python
# 添加更多新闻源
news_sources = [
    {
        "title": "你的新闻标题",
        "date": datetime.now().strftime("%b %d"),
        "tag": "分类标签",
        "summary": "新闻摘要",
        "url": "https://原始链接.com"
    },
    # ...
]
```

## ⏰ 修改定时时间

编辑 `.github/workflows/daily-update.yml`：

```yaml
on:
  schedule:
    # 分 时 日 月 周 (UTC时间)
    # 每天 UTC 02:00 = 北京时间 10:00
    - cron: '0 2 * * *'
    
    # 改为每天 UTC 00:00 = 北京时间 08:00
    # - cron: '0 0 * * *'
    
    # 改为每小时更新一次
    # - cron: '0 * * * *'
```

## 🚨 故障排除

### 更新失败

1. 检查 Actions 日志
2. 确认 `GITHUB_TOKEN` 有写入权限
3. 检查仓库设置中的 Actions 权限

### 内容未更新

- 检查脚本是否正确执行
- 确认有内容变化才会提交
- 查看 GitHub Pages 是否自动部署

## 📊 更新日志

更新记录可在 GitHub 提交历史中查看：
- 自动更新：`📰 Daily update: YYYY-MM-DD`
- 手动更新：`Manual content update`
